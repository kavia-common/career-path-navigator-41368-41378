# Backend environment configuration (example)
# Copy to .env and adjust as needed. Do NOT commit secrets.

# Force SQLite provider by default
DATA_PROVIDER=sqlite

# Optional explicit DB path; defaults to ../../career_navigator_database/myapp.db
# Use absolute path if deploying in a different layout.
# DB_PATH=/home/app/career_navigator_database/myapp.db

# JWT settings (use strong secret in real deployments)
JWT_SECRET=change-me-strong-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000
