# Backend environment example for Career Navigator

# Data provider: 'sqlite' (recommended) or 'json'
DATA_PROVIDER=sqlite

# SQLite DB path (used when DATA_PROVIDER=sqlite)
# Adjust to your environment; service will create directory if missing.
DB_PATH=/absolute/path/to/career_navigator_database/myapp.db

# JWT configuration
JWT_SECRET=replace-with-strong-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS allowed origins (comma-separated). Example for local frontend:
CORS_ORIGINS=http://localhost:3000

# Security: prefer passlib's pure-python bcrypt to avoid C-extension issues in some environments
PASSLIB_BUILTIN_BCRYPT=1
